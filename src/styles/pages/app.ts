import { styled } from "..";

export const ContainerApp = styled('div', {
  display: "flex",
  flexDirection: 'column',
  alignItems: 'flex-start',
  justifyContent: 'center',
  minHeight: '100vh',
})
